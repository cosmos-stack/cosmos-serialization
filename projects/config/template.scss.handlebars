{{#sprites}}
.icon-sprite-{{name}} {
  display: block;
  background-image: url(../images/{{{escaped_image}}});
  background-position: {{px.offset_x}} {{px.offset_y}};
  width: {{px.width}};
  height: {{px.height}};
}
{{/sprites}}
